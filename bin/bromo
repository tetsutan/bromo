#!/usr/bin/env ruby

require 'bromo'

require Bromo::Config.rc_path if Bromo::Config.check_path

Signal.trap(:INT) do
  puts "trap :INT"
  Bromo::Core.stop
end


if Bromo::Config.check_config
  Bromo::Core.start
end

