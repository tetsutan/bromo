#!/usr/bin/env ruby

require 'bromo'

require Bromo::Config.rc_path if Bromo::Config.check_path

if Bromo::Config.check_config
  Bromo::Recorder.start
  # Bromo::Server.run!

  # debug
  sleep 5
  Bromo::Recorder.stop

end

