#!/usr/bin/env ruby

require 'bromo'

require Bromo::Config.rc_path if Bromo::Config.check_path

if Bromo::Config.check_config
  Bromo::Core.start
  # Bromo::Server.run!

  # debug
  sleep 20
  Bromo::Core.stop

end

